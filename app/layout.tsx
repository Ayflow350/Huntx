import type { Metadata } from "next";

import "./globals.css";

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <head>
        <link
          href="https://api.fontshare.com/css?f[]=clash-display@700,500,400&display=swap"
          rel="stylesheet"
        />
      </head>
      <body className={` epilogue-font antialiased`}>{children}</body>
    </html>
  );
}
